package bg.softuni.fundamentals.METHODS.ReadyToUseMethods;

public class convertStringToCamelCase {
    private void setType(String type) {
        type = type.replace(type.charAt(0), Character.toUpperCase(type.charAt(0)));

    }
}
